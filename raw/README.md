# raw/ — raw captures (empty placeholder)

This directory is reserved for raw, unsorted captures (screenshots, dumps,
half-formed notes, imports). Nothing here is expected to be atomic or indexed.

When a capture matures, atomic-ize it into `wiki/<slug>.md` with proper
frontmatter and link it from `index.md`.